import React from 'react';
import './marquee.scss';

interface MarqueeProps {
  text: string[];
}

const Marquee: React.FC<MarqueeProps> = ({ text }): JSX.Element => {

  return (
    <div> </div>
  ); 
}